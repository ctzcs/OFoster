# Framework source map

This directory is the OFoster framework package. It consolidates the translated
runtime into a small number of compilation units so Odin can build the framework
without a project-file generator. The directories here mirror Foster's original
C# layout, making it straightforward to locate the corresponding API.
